// +prop AppID = '1'
     action {
        var params map
        params["Value"] = $Value
        CallContract($Name, params)
        
        DBUpdateSysParam($Name, $Value, $Conditions)
     }
}
